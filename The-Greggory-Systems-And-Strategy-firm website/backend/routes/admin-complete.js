const express=require('express');const router=express.Router();const db=require('../config/database');
router.get('/budget-overview',async(req,res)=>{try{const[pb]=await db.promise().query('SELECT COALESCE(SUM(actual_budget),0) as spent,COALESCE(SUM(estimated_budget),0) as planned FROM user_projects WHERE deleted_at IS NULL');const[fn]=await db.promise().query("SELECT COALESCE(SUM(CASE WHEN entry_type IN ('income','invoice_payment') THEN amount ELSE 0 END),0) as revenue,COALESCE(SUM(CASE WHEN entry_type='expense' THEN amount ELSE 0 END),0) as expenses FROM accounting_entries WHERE deleted_at IS NULL AND payment_status='completed'");const[ac]=await db.promise().query("SELECT COUNT(*) as count FROM user_projects WHERE status IN ('in-progress','active') AND deleted_at IS NULL");const r=fn[0]?.revenue||0;const e=fn[0]?.expenses||0;const p=pb[0]?.planned||0;res.json({success:true,data:{planned:p,spent:e,forecast:e>0?e*1.1:0,revenue:r,expenses:e,net_income:r-e,active_projects:ac[0]?.count||0,remaining:Math.max(0,p-e)}})}catch(err){res.status(500).json({success:false,message:'Budget overview failed'})}});
router.get('/search',async(req,res)=>{try{const{q,deep}=req.query;if(!q||q.length<2)return res.json({success:true,results:[]});const t=`%${q}%`;const l=deep==='true'?20:5;const[u]=await db.promise().query("(SELECT 'user' as type,id,COALESCE(display_name,CONCAT_WS(' ',first_name,last_name),email) as title,email as subtitle,CONCAT('/admin/users/detail/',id,'/client') as link FROM users WHERE (display_name LIKE ? OR email LIKE ?) AND deleted_at IS NULL) UNION ALL (SELECT 'user' as type,id,COALESCE(display_name,CONCAT_WS(' ',first_name,last_name),email) as title,email as subtitle,CONCAT('/admin/users/detail/',id,'/admin') as link FROM admin_users WHERE (display_name LIKE ? OR email LIKE ?) AND deleted_at IS NULL) LIMIT ?",[t,t,t,t,l]);const[p]=await db.promise().query("SELECT 'project' as type,id,project_name as title,client_name as subtitle,'/admin/projects' as link FROM user_projects WHERE (project_name LIKE ? OR client_name LIKE ?) AND deleted_at IS NULL LIMIT ?",[t,t,l]);const[lg]=await db.promise().query("SELECT 'ledger' as type,id,description as title,CONCAT('KSH ',FORMAT(amount,2)) as subtitle,'/admin/billing' as link FROM accounting_entries WHERE (description LIKE ? OR transaction_reference LIKE ?) AND deleted_at IS NULL LIMIT ?",[t,t,l]);const[tk]=await db.promise().query("SELECT 'task' as type,id,task_name as title,status,priority,CONCAT('/admin/projects/',project_id,'/tasks') as link FROM project_tasks WHERE (task_name LIKE ? OR task_description LIKE ?) AND deleted_at IS NULL LIMIT ?",[t,t,l]);res.json({success:true,results:[...u,...p,...lg,...tk]})}catch(err){res.status(500).json({success:false,message:'Search failed'})}});
router.post('/team',async(req,res)=>{try{const{name,role,department,description,email}=req.body;if(!name)return res.status(400).json({error:'Name required'});const[r]=await db.promise().query('INSERT INTO team_members (name,role,department,description,email,is_active,created_at) VALUES (?,?,?,?,?,1,NOW())',[name,role||'member',department||null,description||null,email||null]);res.status(201).json({success:true,id:r.insertId})}catch(err){res.status(500).json({error:'Failed'})}});
router.get('/crm-telemetry',async(req,res)=>{try{const[contacts]=await db.promise().query("SELECT * FROM crm_contacts WHERE deleted_at IS NULL ORDER BY created_at DESC");res.json({success:true,clients:contacts,opportunities:[],pipeline:[{stage:'Leads',count:contacts.filter(c=>c.status==='lead').length,color:'bg-blue-500'},{stage:'Active',count:contacts.filter(c=>c.status==='active').length,color:'bg-green-500'}]})}catch(err){res.status(500).json({success:false,message:'CRM failed'})}});
router.get('/node-settings',async(req,res)=>{try{const[settings]=await db.promise().query('SELECT * FROM admin_settings');const m={};settings.forEach(s=>{m[s.setting_key]=s.setting_value});res.json({success:true,settings:m,system:{status:'operational',uptime:'99.9%'}})}catch(err){res.status(500).json({success:false,message:'Failed'})}});
router.get('/ledger',async(req,res)=>{try{const[e]=await db.promise().query('SELECT * FROM accounting_entries WHERE deleted_at IS NULL ORDER BY created_at DESC LIMIT 100');res.json({success:true,entries:e})}catch(err){res.status(500).json({success:false,message:'Failed'})}});
router.get('/mpesa/transactions',async(req,res)=>{try{const[r]=await db.promise().query('SELECT * FROM mpesa_transactions ORDER BY created_at DESC LIMIT 50');res.json({success:true,transactions:r})}catch(err){res.status(500).json({success:false,message:'Failed'})}});
router.delete('/accounting/entries/:id',async(req,res)=>{try{await db.promise().query('UPDATE accounting_entries SET deleted_at=NOW() WHERE id=?',[req.params.id]);res.json({success:true})}catch(err){res.status(500).json({error:'Failed'})}});
router.delete('/invoices/:id',async(req,res)=>{try{await db.promise().query('UPDATE invoices SET deleted_at=NOW() WHERE id=?',[req.params.id]);res.json({success:true})}catch(err){res.status(500).json({error:'Failed'})}});
router.delete('/blog-articles/:id',async(req,res)=>{try{await db.promise().query('UPDATE blog_articles SET deleted_at=NOW() WHERE id=?',[req.params.id]);res.json({success:true})}catch(err){res.status(500).json({error:'Failed'})}});
module.exports=router;
