import React from 'react';

export function Dashboard({ user }) {
  // Blank white page - admin content removed
  return (
    <div className="min-h-screen bg-white">
      {/* Empty dashboard - navbar handles everything */}
    </div>
  );
}

export default Dashboard;
