# Database backups (contain user data - never commit)
backups/

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Env files (keep .env.example)
.env
.env.*
!.env.example

# Database backups (keep these for GitHub safety)
# backups/ directory is tracked for disaster recovery

# AI/editor agent local config
.kilo/

# NOTE: "The-Greggory-Systems-And-Strategy-firm website/package-lock.json" is
# intentionally NOT ignored anymore: Render builds via `npm --prefix` from this
# lockfile (which pins vite + daisyUI). Keeping it in git makes deploys
# reproducible. Do NOT re-add it to .gitignore.
The-Greggory-Systems-And-Strategy-firm website/scripts/_reseed-data.sql
